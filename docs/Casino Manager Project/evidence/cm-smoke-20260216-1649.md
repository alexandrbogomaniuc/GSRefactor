# CM Smoke Evidence (2026-02-16 16:49 UTC)

## Command
```bash
cd /Users/alexb/Documents/Dev/cm-module
npm run smoke
```

## Key Results
- Health endpoint responded `ok=true`.
- Login with bootstrap credentials `root/root` succeeded.
- `mustChangePassword=true` enforced.
- Protected report call before password change returned:
  - `{"error":"PASSWORD_CHANGE_REQUIRED"}`
- Password change succeeded.
- Re-login with new password succeeded and returned `mustChangePassword=false`.
- Live report data call succeeded:
  - `GET /cm/reports/playerSearch?bankId=6274&limit=5`
  - response included row:
    - `bankid=6274`
    - `extid=bav_game_session_001`
    - `accountid=40962`

## Notes
- `bankList` and `transactions` calls were successful and returned `count=0` with current local dataset for tested filters.
