# Phase 8 Wave 3 CLI Compare Export With Policy Profiles (2026-02-23)

## Scope
- GS-only Phase 8 Wave 3 continuation (main project priority).
- Add parser-side/non-UI compact discrepancy comparison export CLI.
- Reuse the same named threshold policy profiles used in the support viewer.
- No GS runtime/protocol behavior change.

## What Changed
### 1) New CLI compare/export tool (non-UI automation path)
Added scripts:
- `gs-server/deploy/scripts/phase8-precision-wave3-discrepancy-compare-export.sh`
- `gs-server/deploy/scripts/phase8-precision-wave3-discrepancy-compare-export-smoke.sh`

Capabilities:
- reads baseline A + comparison B JSON exports generated by `phase8-precision-wave3-discrepancy-export.sh`
- applies named threshold policy profile:
  - `strict`
  - `canary_gate`
  - `shadow_observe`
  - `demo_sample_pass`
- emits compact comparison JSON report (`phase8-wave3-discrepancy-compare-report`)
- optionally writes Markdown summary report (`--md-out-file`)
- returns summary line for automation/logging (`overall`, `policy`, `metricCount`, `failRules`)

### 2) Verification suite integration
Updated:
- `gs-server/deploy/scripts/phase5-6-local-verification-suite.sh`

New suite coverage:
- CLI help gate for compare/export script + smoke
- deterministic logic smoke validating strict FAIL and demo PASS outcomes, policy names, union metric count, compare-only metric flagging, and Markdown output

### 3) Operator docs / evidence routing
Updated:
- `support/modernizationRunbook.jsp` (adds CLI compare/export reference)
- `support/modernizationDocs.jsp` Phase 8 doc list
- checklist evidence pointer moved to this doc (`doc 101`)

## Test Evidence
- Targeted script checks ✅
  - `bash -n` on both new scripts
  - `phase8-precision-wave3-discrepancy-compare-export.sh --help`
  - `phase8-precision-wave3-discrepancy-compare-export-smoke.sh` (strict FAIL path + demo PASS path)
- Dashboard embedded sync ✅
  - checklist evidence path points to `docs/101-...`
- `gs-server/deploy/scripts/phase5-6-local-verification-suite.sh` ✅
  - report: `docs/quality/local-verification/phase5-6-local-verification-20260223-164534.md`

## Compatibility / Rollback
- CLI tool is additive only; no GS runtime behavior or protocol behavior changed.
- Rollback is isolated to the new scripts, suite entries, and doc/checklist references.

## Next Step
- Optionally add CLI threshold overrides (still seeded by named profiles) and/or viewer import of CLI compare JSON for a unified operator workflow.
