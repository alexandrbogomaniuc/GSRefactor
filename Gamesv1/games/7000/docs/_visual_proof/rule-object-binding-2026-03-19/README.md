# Rule-Object Binding Proof (2026-03-19)

Benchmark path:

- `http://127.0.0.1:8091/?allowDevFallback=1&assetProvider=donorlocal&mathSource=provisional`

Preset captures (fresh in this pass):

- `trigger-collect.png` (`mathPreset=collect`)
- `trigger-boost.png` (`mathPreset=boost`)
- `trigger-jackpot.png` (`mathPreset=jackpot`)

Required reference captures reused from prior approved rule-object-binding proof:

- `help-surface.png`
- `bell-runtime-slot.png`

Reason:

- this pass is focused on applying/validating rule→object→trigger binding completeness without changing approved preloader/asset routing; help and bell captures were already valid and unchanged.
