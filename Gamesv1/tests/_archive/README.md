# Archived Tests

These tests are archived/non-canonical.

- `contract/mock-gs/*`: legacy abs.gs.v1 / ExtGame scenario runner.
- `operator/pariplay.test.ts`: operator-specific integration outside canonical slot-browser-v1 runtime scope.

Canonical runtime contract tests live in:
- `tests/contract/browser-runtime.contract.test.ts`
