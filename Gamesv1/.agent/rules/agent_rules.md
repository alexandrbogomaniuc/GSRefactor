# Agent Project Rules

1. All agent edits must stay inside `e:\Dev\GSRefactor\Gamesv1`.
2. Canonical project context is `docs/MasterContext.md`.
3. If a rule file conflicts with canonical docs, update the rule file immediately; do not keep both versions.
4. Asset pipeline rules must follow `.agent/rules/20_asset_pipeline.md` and canonical docs under `docs/`.
