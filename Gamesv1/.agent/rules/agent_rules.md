# Agent Project Rules

1. All agent edits must stay inside `e:\Dev\GSRefactor\Gamesv1`.
2. Canonical project context is `docs/MasterContext.md`.
3. Canonical capability spec is `docs/GAME_CLIENT_REQUIREMENTS_MAIN.md`.
4. Canonical runtime target is GS HTTP path; WebSocket is legacy/experimental only.
5. Browser transport scope is GS-only; slot-engine internals are server-side/private.
6. If a rule/workflow conflicts with canonical docs, update the rule/workflow immediately.
