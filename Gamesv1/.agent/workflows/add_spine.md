# /add_spine — Spine Runtime Integration (Pixi v8)

When triggered:
1) Add Pixi v8 compatible Spine runtime.
2) Add a wrapper component for Spine playback (play/stop/skin/loop).
3) Add a demo Spine asset loading path (placeholder if no real asset yet).
Acceptance:
- Spine integration compiles.
- Demo scene can instantiate Spine object (or mock until asset exists).
