# GS Contract Helper Schemas (Repo-Generated)

These files are repository-owned helper wrappers used by local tests/tooling.

- `runtime-wire.ts`: local TS schema wrapper for browser runtime payload checks.
- `release-registration.ts`: local TS schema wrapper for release registration checks.

They are non-canonical. Canonical GS contract content is upstream and mirrored under `docs/gs` only.
