import { GsHttpRuntimeTransport } from "./GsHttpRuntimeTransport.ts";

/**
 * @deprecated Use GsHttpRuntimeTransport directly.
 * Kept only as alias for legacy imports while migrating to canonical naming.
 */
export class ExtGameTransport extends GsHttpRuntimeTransport {}
