# @gamesv1/operator-pariplay

Non-canonical optional adapter package.

Status:
- Out of scope for GS phase-1 canonical runtime path.
- Keep only for optional integration experiments.

Canonical browser runtime path:
- `packages/core-protocol` HTTP transport (`/slot/v1/*`)
- Browser -> GS only.
