# Optional Phase 2E Rare States Prompt

## Prompt

Use Git as the audit workflow. Do NOT push to main directly.

This is OPTIONAL PHASE 2E: final validation plus rare-state hunt.

Base:
- Branch from the published canonical donor pack branch.

Required first:
1. Re-run sanitization on the canonical donor folder.
2. Validate any evidence indexes so every referenced file exists.

Optional hunt:
- Only continue if a live probe remains cheap and reliable.
- Use autoplay and buy-bonus probes only when they are already known controls.
- Capture rare-state evidence only if a jackpot-tier, massive overlay, or equivalent special state is clearly observed.
- If nothing is observed, or the tooling path becomes unreliable, stop and write a short negative-result note.

Do not:
- add fake or weak evidence
- commit raw donor binaries
- stretch the optional hunt into a broad new donor run

Stop condition:
- Push the optional follow-up branch and stop.
